version: '3.8'
services:
  dowsing-rod:
    build: .
    env_file: .env
    ports:
      - '6666:6666'
    depends_on:
      - redis
    networks:
      - redis
  redis:
    image: redis:6.0.8-alpine
    networks:
      - redis
networks:
  redis:
    driver: bridge
